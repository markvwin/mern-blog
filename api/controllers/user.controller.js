// controllers is a folder that will hold the logic for the routes because they can get complex.

export const test = (req, res) => {
  // test api route.
  res.json({ message: "API is working!" });
};
